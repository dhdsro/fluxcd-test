apiVersion: apps/v1
kind: Deployment
metadata:
  name: traefik
  namespace: kube-system
spec:
  template:
    spec:
      containers:
        - name: traefik
          args:
            # Keep existing args and add the following if not already present:
            - --providers.kubernetesgateway
